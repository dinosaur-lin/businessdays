(jbuild_version 1)

(library ((name businessdays)
  (public_name businessdays)  
  (flags (:standard -safe-string))
   (preprocess (pps (ppx_jane)))
  (libraries (
    core_kernel
    ppx_jane
  ))
))
